---
const roadmap = [
  { name: "Go", logos: ["/pictures/go-logo.svg"] },
  { name: "Python", logos: ["/pictures/python-logo.svg"] },
  { name: "C#", logos: ["/pictures/csharp-logo.svg"] },
  {
    name: "JavaScript + TypeScript",
    logos: ["/pictures/javascript-logo.svg", "/pictures/typescript-logo.svg"],
  },
];

---

<section class="band technology-band" aria-labelledby="technology-heading">
  <div class="mx-auto max-w-6xl">
    <div class="section-header">
      <p class="section-eyebrow">Analysis coverage</p>
      <h2 id="technology-heading" class="section-heading !text-2xl sm:!text-[2.125rem] md:!text-[2.5rem] lg:!text-[2.875rem]">Supported languages and frameworks</h2>
    </div>

    <div class="section-content !mt-8 overflow-hidden rounded-2xl border border-border-strong bg-background shadow-sm sm:!mt-12 lg:!mt-16 lg:grid lg:grid-cols-[minmax(0,2fr)_minmax(0,3fr)]">
      <section class="px-4 py-6 sm:px-8 sm:py-8 lg:border-r lg:border-border" aria-labelledby="technology-available-heading">
        <h3 id="technology-available-heading" class="font-mono text-[10px] font-semibold uppercase tracking-[0.14em] text-primary">Available now</h3>

        <div class="mt-5 grid grid-cols-2 items-start gap-3 sm:mt-8 sm:items-center sm:gap-8" role="list" aria-label="Available analysis coverage">
          <div class="flex min-w-0 flex-col items-center gap-3 text-center sm:gap-4" role="listitem">
            <span class="flex items-center justify-center" aria-hidden="true">
              <span class="flex h-16 w-16 items-center justify-center rounded-full border border-border bg-white shadow-sm sm:h-16 sm:w-16">
                <img src="/pictures/java-logo.svg" alt="" class="h-12 w-12 object-contain" />
              </span>
              <span class="-ml-2 flex h-16 w-16 items-center justify-center rounded-full border border-border bg-white shadow-sm">
                <img src="/pictures/kotlin-logo.svg" alt="" class="h-12 w-12 object-contain" />
              </span>
            </span>
            <span class="font-mono text-xs font-semibold leading-5 text-foreground sm:text-sm">Java + Kotlin</span>
          </div>

          <a href="/blog/spring-analyzer/" class="flex min-w-0 flex-col items-center gap-3 text-center text-foreground transition-colors hover:text-primary sm:gap-4" role="listitem">
            <span class="flex h-16 w-full items-center justify-center" aria-hidden="true">
              <img src="/pictures/spring-boot-logo.svg" alt="" class="h-14 w-auto max-w-[8rem] object-contain sm:h-12 sm:max-w-[10rem]" />
            </span>
            <span class="font-mono text-xs font-semibold leading-5 sm:text-sm">Spring</span>
          </a>
        </div>
      </section>

      <section class="border-t border-border bg-code-bg px-4 py-6 sm:px-8 sm:py-8 lg:border-t-0" aria-labelledby="technology-roadmap-heading">
        <h3 id="technology-roadmap-heading" class="font-mono text-[10px] font-semibold uppercase tracking-[0.14em] text-muted-foreground">Roadmap</h3>

        <ul class="mt-5 grid grid-cols-2 gap-x-3 gap-y-6 sm:mt-8 sm:grid-cols-4 sm:gap-8" aria-label="Planned language support">
          {roadmap.map((item) => (
            <li class="flex min-w-0 flex-col items-center gap-3 text-center sm:gap-4">
              <span class="flex h-16 min-w-16 items-center justify-center" aria-hidden="true">
                {item.logos.map((logo, index) => (
                  <span class:list={[
                    "flex h-16 w-16 shrink-0 items-center justify-center rounded-full border border-border bg-white shadow-sm",
                    index > 0 && "-ml-2",
                  ]}>
                    <img src={logo} alt="" class="h-12 w-12 object-contain" />
                  </span>
                ))}
              </span>
              <span class="max-w-[8rem] font-mono text-[11px] font-semibold leading-4 text-foreground sm:text-xs sm:leading-5">{item.name}</span>
            </li>
          ))}
        </ul>
      </section>
    </div>
  </div>
</section>
